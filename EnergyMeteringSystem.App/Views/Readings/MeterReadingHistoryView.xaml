<UserControl x:Class="EnergyMeteringSystem.App.Views.Readings.MeterReadingHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Фильтры -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Объект:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{Binding Objects}" 
                      SelectedItem="{Binding SelectedObject}"
                      DisplayMemberPath="Address"
                      Width="250" Margin="5"/>

            <TextBlock Text="Счётчик:" VerticalAlignment="Center" Margin="15,5,5,5"/>
            <ComboBox ItemsSource="{Binding Meters}" 
                      SelectedItem="{Binding SelectedMeter}"
                      DisplayMemberPath="SerialNumber"
                      Width="150" Margin="5"/>
        </StackPanel>

        <!-- Диапазон дат -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="С:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker SelectedDate="{Binding StartDate}" Width="120" Margin="5"/>
            <TextBlock Text="По:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker SelectedDate="{Binding EndDate}" Width="120" Margin="5"/>

            <Button Content="Обновить" Command="{Binding RefreshCommand}" 
                    Padding="10,5" Margin="15,5,5,5"/>
        </StackPanel>

        <!-- График -->
        <Border Grid.Row="2" Background="White" BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,10">
            <lvc:CartesianChart Series="{Binding SeriesCollection}" 
                               LegendLocation="Top" 
                               Margin="10">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Дата" Labels="{Binding ChartDates}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Показания" LabelFormatter="{Binding YFormatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

        <!-- Таблица истории -->
        <DataGrid Grid.Row="3" 
                  ItemsSource="{Binding Readings}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MaxHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding ReadingDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Показание" Binding="{Binding Value}" Width="120"/>
                <DataGridTextColumn Header="Потребление" Binding="{Binding Consumption}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="120"/>
                <DataGridTextColumn Header="Кто ввёл" Binding="{Binding EnteredBy}" Width="150"/>
                <DataGridTextColumn Header="Время" Binding="{Binding EnteredAt, StringFormat={}{0:HH:mm}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>