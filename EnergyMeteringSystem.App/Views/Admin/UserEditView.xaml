<UserControl x:Class="EnergyMeteringSystem.App.Views.Admin.UserEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EnergyMeteringSystem.App.Converters">

    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="True">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Логин -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                         Margin="5" Padding="5"
                         IsReadOnly="{Binding IsSelfEdit}"
                         IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>

                <!-- ФИО -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="ФИО:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                         Margin="5" Padding="5"
                         IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>

                <!-- Email -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Margin="5" VerticalAlignment="Center"/>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical">
                    <TextBox 
                        Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        Margin="5" Padding="5"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                    <TextBlock Text="{Binding EmailError}" 
                               Foreground="Red" 
                               FontSize="11"
                               Margin="5,2,5,5"
                               Visibility="{Binding ShowEmailError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Роль -->
                <!-- Роль -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Роль:" Margin="5" VerticalAlignment="Center"/>

                <!-- ComboBox - когда админ редактирует другого пользователя -->
                <ComboBox Grid.Row="3" Grid.Column="1" 
          ItemsSource="{Binding Roles}"
          SelectedItem="{Binding SelectedRole}"
          DisplayMemberPath="Name"
          Margin="5" Padding="5"
          Visibility="{Binding ShowRolePicker, Converter={StaticResource BooleanToVisibilityConverter}}"
          IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>

                <!-- TextBlock - когда пользователь редактирует себя (или админ себя) -->
                <TextBlock Grid.Row="3" Grid.Column="1" 
           Text="{Binding SelectedRole.Name, TargetNullValue=Не выбрана}"
           Margin="5" Padding="5"
           VerticalAlignment="Center"
           Visibility="{Binding IsSelfEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Новый пароль (только для саморедактирования) -->
                <TextBlock Grid.Row="4" Grid.Column="0" 
                           Text="Новый пароль:" 
                           Margin="5" VerticalAlignment="Center"
                           Visibility="{Binding IsSelfEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <PasswordBox x:Name="NewPasswordBox" 
                             Grid.Row="4" Grid.Column="1" 
                             Margin="5" Padding="5"
                             PasswordChanged="NewPasswordBox_PasswordChanged"
                             IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                             Visibility="{Binding IsSelfEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Подтверждение пароля -->
                <TextBlock Grid.Row="5" Grid.Column="0" 
                           Text="Подтверждение:" 
                           Margin="5" VerticalAlignment="Center"
                           Visibility="{Binding IsSelfEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <PasswordBox x:Name="ConfirmPasswordBox" 
                             Grid.Row="5" Grid.Column="1" 
                             Margin="5" Padding="5"
                             PasswordChanged="ConfirmPasswordBox_PasswordChanged"
                             IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                             Visibility="{Binding IsSelfEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Ошибка пароля -->
                <TextBlock Grid.Row="6" Grid.Column="1" 
                           Text="{Binding PasswordError}"
                           Foreground="Red"
                           FontSize="11"
                           Margin="5,2,5,5"
                           Visibility="{Binding HasPasswordError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Ошибка -->
                <TextBlock Grid.Row="7" Grid.Column="1"
                           Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           FontSize="11"
                           Margin="5,2,5,5"
                           TextWrapping="Wrap"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Кнопки -->
                <StackPanel Grid.Row="9" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Сохранить" 
                            Command="{Binding SaveCommand}"
                            Padding="20,8" Margin="5"
                            Background="#4CAF50" 
                            Foreground="White"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#A5D6A7"/>
                                        <Setter Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Отмена" 
                            Command="{Binding CancelCommand}"
                            Padding="20,8" Margin="5"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Индикатор загрузки (оверлей) -->
        <Border Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="20"/>
                <TextBlock Text="Сохранение..." Margin="0,10,0,0" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>