<UserControl x:Class="EnergyMeteringSystem.App.Views.Admin.UserEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EnergyMeteringSystem.App.Views.Admin">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Логин -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Username}" 
                 Margin="5" Padding="5"/>

        <!-- ФИО -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="ФИО:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FullName}" Margin="5" Padding="5"/>

        <!-- Email с маской ввода через PreviewTextInput -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Margin="5" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical">
            <TextBox 
                Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                Margin="5" Padding="5"
                ToolTip="Формат: имя@домен.ру (только латиница, цифры и @._%-)"
                PreviewTextInput="TextBox_PreviewTextInput"
                PreviewKeyDown="TextBox_PreviewKeyDown"
                LostFocus="TextBox_LostFocus"
                GotFocus="TextBox_GotFocus"/>

            <TextBlock Text="Формат: имя@домен.ру (например: ivan@mail.ru)" 
                       Foreground="#666" 
                       FontSize="10"
                       Margin="5,0,5,5"/>
        </StackPanel>

        <!-- Роль -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Роль:" Margin="5" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" 
                  ItemsSource="{Binding Roles}"
                  SelectedItem="{Binding SelectedRole}"
                  DisplayMemberPath="Name"
                  Margin="5" Padding="5"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Padding="20,8" Margin="5"
                    Background="#4CAF50" Foreground="White"/>
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Padding="20,8" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>