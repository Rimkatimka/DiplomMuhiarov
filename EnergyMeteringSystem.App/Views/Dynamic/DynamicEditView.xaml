<!-- EnergyMeteringSystem.App/Views/Dynamic/DynamicEditView.xaml -->
<Window x:Class="EnergyMeteringSystem.App.Views.Dynamic.DynamicEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="300"
        MinWidth="400">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Контент формы (генерируется динамически) -->
        <ScrollViewer Grid.Row="0" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ContentControl Content="{Binding FormGrid}" />
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button x:Name="SaveButton"
                    Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Padding="20,8" 
                    Margin="5"
                    Background="#4CAF50" 
                    Foreground="White"
                    Cursor="Hand"/>
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Padding="20,8" 
                    Margin="5"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>